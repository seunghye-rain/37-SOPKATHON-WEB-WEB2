import AppBar from '@/shared/components/app-bar/app-bar';

const Home = () => {
  return (
    <>
      <AppBar variant='default' />
      <AppBar variant='onboarding' />
    </>
  );
};

export default Home;
